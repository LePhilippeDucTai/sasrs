# Toolchain épinglée (J01-P2) : fmt/clippy et les lints ne changent plus
# avec la toolchain — la CI locale et GitHub Actions compilent la même chose.
[toolchain]
channel = "1.98.1"
components = ["rustfmt", "clippy"]
